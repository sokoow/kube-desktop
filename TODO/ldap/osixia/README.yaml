---
#
# This is the canonical configuration for the `README.md`
# Run `make readme` to rebuild the `README.md`
#

# Name of this project
name: Docker OpenLDAP

# Short description of this project
description: |-
  Dockerfile for OpenLDAP

# Canonical GitHub repo
github_repo: UnionPOS/docker-openldap

# How to get started quickly
quickstart: |-

  Here's how to get started...

  1. ensure you have a [baseline configuration](https://github.com/UnionPOS/baseline/)
  1. `git clone https://github.com/UnionPOS/docker-openldap` to pull down the repository
  1. `make init` to initialize the [`build-harness`](https://github.com/UnionPOS/build-harness/)

# How to use this project
usage: |-
  ### Building the image

  ```sh
  make build
  ```

  ### Running the container

  ```bash
  make run
  ```

  ### Running the container (interactive mode)

  ```bash
  make it
  ```

# References
references:
  - name: "Docker"
    description: "Docker Documentation"
    url: "https://docs.docker.com/"
